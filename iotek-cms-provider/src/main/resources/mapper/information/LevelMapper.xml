<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iotekclass.cms.persist.information.LevelMapper">


  	
  	<select id="getLevelAll" resultType="InformationSort">
  		SELECT * FROM ${tableName} where parent_id=#{levelId}
  	</select>
	<select id="getLevelSeo" resultType="InformationSort">
  		SELECT * FROM ${tableName} where parent_id=#{levelId}
  	</select>
	
</mapper>